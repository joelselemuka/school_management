from dateutil.relativedelta import relativedelta
class AcademicRules:
    MIN_YEAR_DURATION = relativedelta(months=8)
    MAX_PERIOD_DURATION = relativedelta(months=4)
    MAX_PERIODS_PER_TRIMESTRE = 3